function fiftyThirtyTwenty(ati) {
var	needs= (ati/100)*50;
var wants= (ati/100)*30;
var savings= (ati/100)*20;

return ["Needs: " + needs + ", " + 
    "Wants: " + wants + ", " +
    "Savings: " + savings ];
}
